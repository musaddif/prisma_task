# Cloudflare Pages — use with Root directory = frontend
# Dashboard: Build command `npm run build`, Output directory `dist`
name = "uproot-clean"
compatibility_date = "2024-09-23"
pages_build_output_dir = "dist"
